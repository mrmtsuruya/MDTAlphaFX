# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Env
.env
.env.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
specs/.current-spec
**/.progress.md

# Historical market data (Dukascopy). Bulk, regenerable, and gigabytes per year
# on XAUUSD ticks -- never commit it. Rebuild with tools/fetch-history.mjs.
data/history/
.dukascopy-cache/

# Local backups
MDTAlphaFX-BACKUP-*.tar.gz

# Local preview tooling runtime state — a ~14MB SQLite db plus its WAL/SHM.
# Machine-local and regenerable; nothing here belongs in a synced repo.
.freebuff/

# Supabase CLI link state (project ref + keys written by `supabase link`).
supabase/.temp/

# Isolated feature worktrees managed by Superpowers/Codex.
.worktrees/
