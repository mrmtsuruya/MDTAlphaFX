# Forex Signals AI

I am trying to build multi featured SaaS called "MDTAlphaFX wherein it is providing a very high quality probability signals for all forex pairs. This contains almost all 28 strategies best source code and configuration, which can be enabled or disabled when we try to try generate signals on demand or using market scanner using the strategies we only want, though using all 28 strategies enabled will still provide best confluence as it can detect whichever strategies are applicable in the current market condition from all timeframes while considering the live news or macro geo-political news that may impact the forex pairs price movement real-time. the dashboard should at least contain Market Overview, signal center, live chart running real-time, strategy tester, backtester. I want to also wire additional dashboard how my daily routine like getting monitoring my computer usage and temp of cpu, disk, memory, video card, including network utilization, etc.

Alongside that, i also want to get a tab for AI news like vibe coding public project ideas, new AI models, real-time top 10 AI model rankings based on verified benchmarks.

I also want to integrate CLI capable cloud models like claude code cli, codex cli, cursor, etc with the dashboard. The reason behind this is because there is a generated signal for each pair, and if i want to consult with any of the cloud models like claude, or codex - i can pacify for them to provide analysis if i will take the signal or not.

meaning, we wire the cloud models within the dashboard and will just authenticate just using the account subscription we have.

Since we have that feature, i also want to see the token usage, credit usage for each of them, graphs, etc...ho

Lastly, i want another feature or tab which combines all of my cloud models once they are all authenticated. meaning, i want it to function as vibe coding so it is more intelligent and smarter. this is similarly something like a base44, lovable, bolt, and hercules functions, and features works. so when a non-technical user uses it for instance: i like to build a delivery app, then it prompts the user accordingly to help as much as possible.

With the front-end design, i want it to be very modern and latest like the cyberpunk style, framing with special motions in transitions when i'm navigating or clicking within the UI. something like that would make the UI usage more exciting.

The last thing with the forex signal feature is automate the received signal via metatrader 5 desktop app, so the user just login either with their demo or real account, and have a setting parameters of the received signal accordingly.

Within the forex signal provider, we should have options for intra-day, and a scalper trader. it logs as well generated signal and tells the user if it's still valid or not, and also tells the user if it's too late and do no chase. generated signals will automate it based on ATR, in terms of stop loss, take profit #1 and takeprofit #2, and summarized technical analysis from the strategies being used including live news, macro geopolitical events that can affect prices, make price more volatile,  and manipulated that may potentially invalidate the signal . Therefore, this signal may either validate the signal or invalidate the technical analysis, so we need to ensure that the live chart prices are very accurate and real-time as this will be useless if it's a different price. back-testing inside the dashboard is must similar on how backtest are performed from metatrader 5, monte-carlo to support the functionality and further enhancement, and optimization for each strategies, so it may prompt the user if we want to optimize the strategies based on the last the last backtest / benchmark.

This project was built with [Lovable](https://lovable.dev).

## Build with Lovable

Continue developing this project in the [Lovable editor](https://lovable.dev/projects/df1e2f17-28fc-447f-b1cf-538639511190).

- **Ship faster**: describe what you want to build and Lovable handles the code.
- **Stay in sync**: every change made in Lovable is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally? You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
